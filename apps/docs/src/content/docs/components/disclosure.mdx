---
title: Disclosure
description: Native details and summary semantics for expandable content.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add disclosure
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import {
  Disclosure,
  DisclosureSummary,
  DisclosureContent,
} from '@simurgh-ui/react/disclosure';
import '@simurgh-ui/styles/disclosure.css';
```

### Vue

```ts
import {
  Disclosure,
  DisclosureSummary,
  DisclosureContent,
} from '@simurgh-ui/vue/disclosure';
import '@simurgh-ui/styles/disclosure.css';
```

### Angular

```ts
import {
  DisclosureComponent,
  DisclosureSummaryDirective,
  DisclosureContentDirective,
} from '@simurgh-ui/angular/disclosure';
import '@simurgh-ui/styles/disclosure.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

## Purpose

Disclosure builds on native `<details>` and `<summary>`, preserving browser keyboard interaction, find-in-page behavior, and no-JavaScript fallback while exposing framework-native state events.

## Anatomy

The public component parts are listed in API surface below. Use only the parts needed by the example;
compound components depend on their documented parent/child nesting.

## Basic usage

<ComponentPreview title="Disclosure">
  <details data-slot="disclosure">
    <summary data-slot="disclosure-summary">Why use native disclosure?</summary>
    <div data-slot="disclosure-content">
      It remains interactive before hydration and follows the browser's
      accessibility model.
    </div>
  </details>
</ComponentPreview>

## Examples

<Tabs>
<TabItem label="React">
```tsx
<Disclosure onOpenChange={setOpen}>
  <DisclosureSummary>What is Simurgh?</DisclosureSummary>
  <DisclosureContent>A framework-neutral UI toolkit.</DisclosureContent>
</Disclosure>
```
</TabItem>
<TabItem label="Vue">
```vue
<Disclosure v-model="open">
  <DisclosureSummary>What is Simurgh?</DisclosureSummary>
  <DisclosureContent>A framework-neutral UI toolkit.</DisclosureContent>
</Disclosure>
```
</TabItem>
<TabItem label="Angular">
```html
<simurgh-disclosure [(open)]="open">
  <summary simurghDisclosureSummary>What is Simurgh?</summary>
  <div simurghDisclosureContent>A framework-neutral UI toolkit.</div>
</simurgh-disclosure>
```
</TabItem>
</Tabs>

{/* component-state-model:start */}
## State model

Controlled state remains the application’s source of truth and must be updated from every change
event. Uncontrolled state reads its default only when the component mounts. Do not pass both forms of
the same state at once.

| Framework | Exact state contract and reset behavior |
| --- | --- |
| React | Controlled: `open` + `onOpenChange`. Uncontrolled: `defaultOpen`. Reset controlled state by assigning the initial value; reset uncontrolled state by changing a React `key`. |
| Vue | Controlled: `v-model` (`modelValue` + `update:modelValue`). No uncontrolled prop is exposed. Reset the model ref to its initial value; reset uncontrolled state with a changed Vue `:key`. |
| Angular | Controlled two-way binding: `[(open)]` (`open` + `openChange`). Angular exposes no separate default input; initialize and reset the bound class field explicitly. |

**React**

```tsx
const initialOpen = false;
const [open, setOpen] = useState(initialOpen);
const [resetKey, setResetKey] = useState(0);

<Disclosure open={open} onOpenChange={setOpen}>…</Disclosure>
<button onClick={() => setOpen(initialOpen)}>Reset controlled</button>
<Disclosure key={resetKey} defaultOpen={initialOpen}>…</Disclosure>
<button onClick={() => setResetKey((key) => key + 1)}>Reset uncontrolled</button>
```

**Vue**

```vue
<script setup lang="ts">
import { ref } from 'vue';
const initial = '';
const state = ref(initial);
const resetKey = ref(0);
</script>

<Disclosure v-model="state">…</Disclosure>
<button @click="state = initial">Reset controlled</button>
```

**Angular**

```ts
initial = false;
open = this.initial;
reset() { this.open = this.initial; }
```

```html
<simurgh-disclosure [(open)]="open">…</simurgh-disclosure>
<button type="button" (click)="reset()">Reset</button>
```
{/* component-state-model:end */}

Use Disclosure for independent expandable information. Use Accordion when several related sections need coordinated open state.

## API surface

- React exports: `Disclosure`, `DisclosureSummary`, `DisclosureContent`
- Vue exports: `Disclosure`, `DisclosureSummary`, `DisclosureContent`
- Angular exports: `DisclosureComponent`, `DisclosureSummaryDirective`, `DisclosureContentDirective`

Import these public symbols from the component subpath shown in Installation. Framework-specific
props, events, slots, directives, methods, defaults, and native-attribute behavior belong in the API
tables on this page; use the linked source only to verify the current implementation.

{/* react-api:start */}
### React

An inherited-attributes entry means the component accepts the complete named React native interface,
including its event handlers and ARIA and data attributes. Remaining attributes are forwarded to the
rendered element unless the component behavior described on this page overrides them.

### `Disclosure`

Inherited attributes: `DetailsHTMLAttributes<HTMLDetailsElement> & RefAttributes<HTMLDetailsElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `defaultOpen` | `boolean` | `undefined` |
| `onOpenChange` | `(open: boolean) => void` | `undefined` |
| `open` | `boolean` | `undefined` |
| `ref` | `Ref<HTMLDetailsElement> \| undefined` | `undefined` |

### `DisclosureSummary`

Inherited attributes: `HTMLAttributes<HTMLElement> & RefAttributes<HTMLElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `ref` | `Ref<HTMLElement> \| undefined` | `undefined` |

### `DisclosureContent`

Inherited attributes: `HTMLAttributes<HTMLDivElement> & RefAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `ref` | `Ref<HTMLDivElement> \| undefined` | `undefined` |
{/* react-api:end */}

{/* vue-api:start */}
### Vue

Boolean props without explicit defaults use Vue's `false` default. Undeclared attributes follow
the fallthrough behavior stated for each component.

### `Disclosure`

Automatic fallthrough is disabled; `attrs` are manually forwarded to the rendered root.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `modelValue` | `boolean` | `undefined` |
| `defaultOpen` | `boolean` | `false` |

| Event | Payload |
| --- | --- |
| `update:modelValue` | `boolean` |

Slots: `default`.

Exposed methods: none.

### `DisclosureSummary`

Vue attribute fallthrough is enabled for the rendered root.

No declared props.

No emitted events.

Slots: `default`.

Exposed methods: none.

### `DisclosureContent`

Vue attribute fallthrough is enabled for the rendered root.

No declared props.

No emitted events.

Slots: `default`.

Exposed methods: none.
{/* vue-api:end */}

{/* angular-api:start */}
### Angular

Inputs and outputs use their public template names. Public methods are callable through a template
reference or `ViewChild`. Native attributes apply to the documented template root or directive host;
they are not automatically forwarded through component hosts.

### `DisclosureComponent`

Component selector: `simurgh-disclosure`. The template's first native element is `details`.

| Input | Type | Default / requirement |
| --- | --- | --- |
| `open` | `boolean` | `false` |

| Output | Payload |
| --- | --- |
| `openChange` | `boolean` |

Content projection: `default`.

| Public method |
| --- |
| `onToggle(event: Event): void` |

### `DisclosureSummaryDirective`

Attribute directive selector: `summary[simurghDisclosureSummary]`; behavior applies to its host element.

No inputs.

No outputs.

Content projection: none.

No public methods.

### `DisclosureContentDirective`

Attribute directive selector: `[simurghDisclosureContent]`; behavior applies to its host element.

No inputs.

No outputs.

Content projection: none.

No public methods.
{/* angular-api:end */}

{/* component-composition:start */}
### Composition contract

Follow the framework example as a structural contract. Root components own shared state; parts that
consume that state must stay under the root (or be attached to projected descendants in Angular).
Parts described as conditional are optional until their corresponding interaction or semantic region
is used. Do not render a state-consuming part by itself.

| Framework | Required parent/child relationship | Conditional parts | Supporting types |
| --- | --- | --- | --- |
| React | Render `Disclosure` as the ancestor. Nest the listed parts inside that root so they can read its shared state/context. | `DisclosureSummary`, `DisclosureContent` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |
| Vue | Render `Disclosure` as the ancestor. Nest the listed parts inside that root so they can read its shared state/context. | `DisclosureSummary`, `DisclosureContent` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |
| Angular | Create `DisclosureComponent` first. Attach listed directives to elements inside its projected content; nested component parts must remain inside it. | `DisclosureSummaryDirective`, `DisclosureContentDirective` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |

Accessible names, descriptions, and form labels remain required whenever the component's purpose
cannot otherwise be determined, even when the corresponding visual part is optional.
{/* component-composition:end */}

{/* framework-parity:start */}
### Framework parity and differences

The adapters target the same user-visible behavior and accessibility contract. Their public shapes
follow each framework's conventions and are not expected to be symbol-for-symbol identical.

| Concern | Contract |
| --- | --- |
| Public composition | All adapters export 3 public symbols, with framework-idiomatic names. |
| State and change events | React uses controlled/default props and callbacks for `open`; Vue uses `v-model`; Angular uses `[(open)]`. |
| Children and content | React uses `children`; Vue uses the slots listed above; Angular uses the documented content projection selectors. |
| Native attributes | React forwards the named native interface; Vue follows the stated fallthrough rule; Angular attributes apply to the component host unless an input, directive, or documented native root consumes them. |
| Imperative access | React forwards native refs; Vue exposes no methods; Angular exposes no public methods. |

These differences are intentional adapter design. Behavioral or accessibility differences not stated
on this page are parity defects rather than supported variations.
{/* framework-parity:end */}

{/* component-form:start */}
### Form integration

This component is not a form control and does not contribute a named value to `FormData`. Use it
to structure or describe a form only when its purpose and accessibility guidance apply.
{/* component-form:end */}

## Customization

Load the optional component stylesheet for the default recipe, override semantic tokens for broad
theme changes, or omit the recipe CSS for headless styling. See [theming and styling
hooks](/guides/theming/) for import order, selectors, dark mode, RTL, and reduced-motion guidance.

## Accessibility

Preserve the documented composition and accessible names when wrapping or restyling this component.
See [accessibility and RTL guidance](/guides/accessibility-rtl/) for keyboard, focus, labeling, and
directionality requirements.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Related components

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/disclosure.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/disclosure.ts).
