---
title: Toolbar
description: A named command group with orientation- and direction-aware keyboard navigation.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add toolbar
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import {
  Toolbar,
  ToolbarButton,
} from '@simurgh-ui/react/toolbar';
import '@simurgh-ui/styles/toolbar.css';
```

### Vue

```ts
import {
  Toolbar,
  ToolbarButton,
} from '@simurgh-ui/vue/toolbar';
import '@simurgh-ui/styles/toolbar.css';
```

### Angular

```ts
import {
  ToolbarComponent,
  ToolbarButtonDirective,
} from '@simurgh-ui/angular/toolbar';
import '@simurgh-ui/styles/toolbar.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

## Purpose

Toolbar groups related controls and moves focus with logical arrow keys while skipping disabled items.

## Anatomy

The public component parts are listed in API surface below. Use only the parts needed by the example;
compound components depend on their documented parent/child nesting.

## Basic usage

<ComponentPreview title="Toolbar">
  <div
    role="toolbar"
    aria-label="Text formatting"
    aria-orientation="horizontal"
    data-slot="toolbar"
  >
    <button data-toolbar-item data-slot="toolbar-button">
      Bold
    </button>
    <button data-toolbar-item data-slot="toolbar-button">
      Italic
    </button>
    <button data-toolbar-item data-slot="toolbar-button">
      Link
    </button>
  </div>
</ComponentPreview>

## Examples

<Tabs>
  <TabItem label="React">
    ```tsx
    <Toolbar label="Text formatting">
      <ToolbarButton>Bold</ToolbarButton>
      <ToolbarButton>Italic</ToolbarButton>
    </Toolbar>
    ```
  </TabItem>
  <TabItem label="Vue">
    ```vue
    <Toolbar label="Text formatting">
      <ToolbarButton>Bold</ToolbarButton>
      <ToolbarButton>Italic</ToolbarButton>
    </Toolbar>
    ```
  </TabItem>
  <TabItem label="Angular">
    ```html
    <simurgh-toolbar label="Text formatting">
      <button simurghToolbarButton>Bold</button>
      <button simurghToolbarButton>Italic</button>
    </simurgh-toolbar>
    ```
  </TabItem>
</Tabs>

## State model

Set `orientation="vertical"` for vertical command groups. Direction-aware horizontal navigation reverses appropriately in RTL layouts.

## API surface

- React exports: `Toolbar`, `ToolbarButton`
- Vue exports: `Toolbar`, `ToolbarButton`
- Angular exports: `ToolbarComponent`, `ToolbarButtonDirective`

Import these public symbols from the component subpath shown in Installation. Framework-specific
props, events, slots, directives, methods, defaults, and native-attribute behavior belong in the API
tables on this page; use the linked source only to verify the current implementation.

{/* react-api:start */}
### React

An inherited-attributes entry means the component accepts the complete named React native interface,
including its event handlers and ARIA and data attributes. Remaining attributes are forwarded to the
rendered element unless the component behavior described on this page overrides them.

### `Toolbar`

Inherited attributes: `HTMLAttributes<HTMLDivElement> & RefAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `direction` | `Direction` | `'ltr'` |
| `label` | `string` | `'Toolbar'` |
| `orientation` | `Orientation` | `'horizontal'` |
| `ref` | `Ref<HTMLDivElement> \| undefined` | `undefined` |

### `ToolbarButton`

Inherited attributes: `ButtonHTMLAttributes<HTMLButtonElement> & RefAttributes<HTMLButtonElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `ref` | `Ref<HTMLButtonElement> \| undefined` | `undefined` |
{/* react-api:end */}

{/* vue-api:start */}
### Vue

Boolean props without explicit defaults use Vue's `false` default. Undeclared attributes follow
the fallthrough behavior stated for each component.

### `Toolbar`

Vue attribute fallthrough is enabled for the rendered root.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `orientation` | `Orientation` | `'horizontal'` |
| `direction` | `Direction` | `'ltr'` |
| `label` | `string` | `'Toolbar'` |

No emitted events.

Slots: `default`.

Exposed methods: none.

### `ToolbarButton`

Vue attribute fallthrough is enabled for the rendered root.

No declared props.

No emitted events.

Slots: `default`.

Exposed methods: none.
{/* vue-api:end */}

{/* angular-api:start */}
### Angular

Inputs and outputs use their public template names. Public methods are callable through a template
reference or `ViewChild`. Native attributes apply to the documented template root or directive host;
they are not automatically forwarded through component hosts.

### `ToolbarComponent`

Component selector: `simurgh-toolbar`. The template's first native element is `ng-content`. Angular host bindings: `{ role: 'toolbar', 'data-slot': 'toolbar', '[attr.aria-label]': 'label', '[attr.aria-orientation]': 'orientation', '[attr.dir]': 'direction', }`.

| Input | Type | Default / requirement |
| --- | --- | --- |
| `label` | `string` | `'Toolbar'` |
| `orientation` | `Orientation` | `'horizontal'` |
| `direction` | `Direction` | `'ltr'` |

No outputs.

Content projection: `default`.

| Public method |
| --- |
| `onKeyDown(event: KeyboardEvent): void` |

### `ToolbarButtonDirective`

Attribute directive selector: `button[simurghToolbarButton]`; behavior applies to its host element.

No inputs.

No outputs.

Content projection: none.

No public methods.
{/* angular-api:end */}

{/* framework-parity:start */}
### Framework parity and differences

The adapters target the same user-visible behavior and accessibility contract. Their public shapes
follow each framework's conventions and are not expected to be symbol-for-symbol identical.

| Concern | Contract |
| --- | --- |
| Public composition | All adapters export 2 public symbols, with framework-idiomatic names. |
| State and change events | No controlled state contract; use native attributes, inputs, or events documented above. |
| Children and content | React uses `children`; Vue uses the slots listed above; Angular uses the documented content projection selectors. |
| Native attributes | React forwards the named native interface; Vue follows the stated fallthrough rule; Angular attributes apply to the component host unless an input, directive, or documented native root consumes them. |
| Imperative access | React forwards native refs; Vue exposes no methods; Angular exposes no public methods. |

These differences are intentional adapter design. Behavioral or accessibility differences not stated
on this page are parity defects rather than supported variations.
{/* framework-parity:end */}

## Customization

Load the optional component stylesheet for the default recipe, override semantic tokens for broad
theme changes, or omit the recipe CSS for headless styling. See [theming and styling
hooks](/guides/theming/) for import order, selectors, dark mode, RTL, and reduced-motion guidance.

## Accessibility

Preserve the documented composition and accessible names when wrapping or restyling this component.
See [accessibility and RTL guidance](/guides/accessibility-rtl/) for keyboard, focus, labeling, and
directionality requirements.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Related components

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/toolbar.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/toolbar.ts).
