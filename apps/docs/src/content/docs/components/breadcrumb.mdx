---
title: Breadcrumb
description: A named navigation landmark for hierarchical location.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add breadcrumb
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import { Breadcrumb } from '@simurgh-ui/react/breadcrumb';
import '@simurgh-ui/styles/breadcrumb.css';
```

### Vue

```ts
import { Breadcrumb } from '@simurgh-ui/vue/breadcrumb';
import '@simurgh-ui/styles/breadcrumb.css';
```

### Angular

```ts
import { BreadcrumbComponent } from '@simurgh-ui/angular/breadcrumb';
import '@simurgh-ui/styles/breadcrumb.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

## Purpose

Breadcrumb supplies the named navigation landmark while consumers retain native links, ordered-list structure, separators, and current-page markup.

## Anatomy

The public component parts are listed in API surface below. Use only the parts needed by the example;
compound components depend on their documented parent/child nesting.

## Basic usage

<ComponentPreview title="Breadcrumb">
  <nav aria-label="Breadcrumb">
    <ol class="preview-row" style="list-style: none; padding: 0; margin: 0">
      <li>
        <a href="#">Docs</a>
      </li>
      <li aria-hidden="true">/</li>
      <li>
        <a href="#">Components</a>
      </li>
      <li aria-hidden="true">/</li>
      <li>
        <span aria-current="page">Breadcrumb</span>
      </li>
    </ol>
  </nav>
</ComponentPreview>

## Examples

<Tabs>
<TabItem label="React">
```tsx
<Breadcrumb><ol>{items}</ol></Breadcrumb>
```
</TabItem>
<TabItem label="Vue">
```vue
<Breadcrumb><ol><li v-for="item in items">{{ item.label }}</li></ol></Breadcrumb>
```
</TabItem>
<TabItem label="Angular">
```html
<simurgh-breadcrumb><ol><li *ngFor="let item of items">{{ item.label }}</li></ol></simurgh-breadcrumb>
```
</TabItem>
</Tabs>

## State model

Mark the final item with `aria-current="page"`. Separators should be decorative and hidden from assistive technology.

## API surface

- React exports: `Breadcrumb`
- Vue exports: `Breadcrumb`
- Angular exports: `BreadcrumbComponent`

Import these public symbols from the component subpath shown in Installation. Framework-specific
props, events, slots, directives, methods, defaults, and native-attribute behavior belong in the API
tables on this page; use the linked source only to verify the current implementation.

{/* react-api:start */}
### React

An inherited-attributes entry means the component accepts the complete named React native interface,
including its event handlers and ARIA and data attributes. Remaining attributes are forwarded to the
rendered element unless the component behavior described on this page overrides them.

### `Breadcrumb`

Inherited attributes: `HTMLAttributes<HTMLElement> & RefAttributes<HTMLElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `label` | `string` | `'Breadcrumb'` |
| `ref` | `Ref<HTMLElement> \| undefined` | `undefined` |
{/* react-api:end */}

{/* vue-api:start */}
### Vue

Boolean props without explicit defaults use Vue's `false` default. Undeclared attributes follow
the fallthrough behavior stated for each component.

### `Breadcrumb`

Vue attribute fallthrough is enabled for the rendered root.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `label` | `string` | `'Breadcrumb'` |

No emitted events.

Slots: `default`.

Exposed methods: none.
{/* vue-api:end */}

## Customization

Load the optional component stylesheet for the default recipe, override semantic tokens for broad
theme changes, or omit the recipe CSS for headless styling. See [theming and styling
hooks](/guides/theming/) for import order, selectors, dark mode, RTL, and reduced-motion guidance.

## Accessibility

Preserve the documented composition and accessible names when wrapping or restyling this component.
See [accessibility and RTL guidance](/guides/accessibility-rtl/) for keyboard, focus, labeling, and
directionality requirements.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Related components

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/breadcrumb.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/breadcrumb.ts).
