---
title: Number Input
description: Native numeric entry with accessible increment and decrement controls.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

<!-- component-installation:start -->
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add number-input
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import { NumberInput } from '@simurgh-ui/react/number-input';
import '@simurgh-ui/styles/number-input.css';
```

### Vue

```ts
import { NumberInput } from '@simurgh-ui/vue/number-input';
import '@simurgh-ui/styles/number-input.css';
```

### Angular

```ts
import { NumberInputComponent } from '@simurgh-ui/angular/number-input';
import '@simurgh-ui/styles/number-input.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
<!-- component-installation:end -->

Number Input keeps native spinbutton semantics while providing larger, explicitly named step controls. Values are clamped to the configured minimum and maximum.

<ComponentPreview title="Number Input">
  <label for="quantity-preview" style="display:block;margin-bottom:.5rem">
    Quantity
  </label>
  <div data-slot="number-input">
    <button
      type="button"
      data-slot="number-input-decrement"
      aria-label="Decrease value"
      aria-controls="quantity-preview"
    >
      −
    </button>
    <input
      id="quantity-preview"
      type="number"
      data-slot="number-input-control"
      value="2"
      min="0"
      max="10"
      step="1"
    />
    <button
      type="button"
      data-slot="number-input-increment"
      aria-label="Increase value"
      aria-controls="quantity-preview"
    >
      +
    </button>
  </div>
</ComponentPreview>

<Tabs>
<TabItem label="React">
```tsx
<NumberInput
  aria-label="Quantity"
  value={quantity}
  min={0}
  max={10}
  onValueChange={setQuantity}
/>
```
</TabItem>
<TabItem label="Vue">
```vue
<NumberInput v-model="quantity" aria-label="Quantity" :min="0" :max="10" />
```
</TabItem>
<TabItem label="Angular">
```html
<simurgh-number-input
  [(value)]="quantity"
  aria-label="Quantity"
  [min]="0"
  [max]="10"
/>
```
</TabItem>
</Tabs>

Use a visible label when possible. Set `step` to the smallest meaningful change, and provide both `min` and `max` when the value has a bounded domain.

<!-- doc-verification:start -->
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
<!-- doc-verification:end -->

## Further guidance

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/number-input.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/number-input.ts).
