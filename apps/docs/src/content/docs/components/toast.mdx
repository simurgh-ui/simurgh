---
title: Toast
description: Timed status announcements with explicit dismissal.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add toast
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import {
  ToastProvider,
  useToast,
  ToastViewport,
  ToastMessage,
} from '@simurgh-ui/react/toast';
import '@simurgh-ui/styles/toast.css';
```

### Vue

```ts
import {
  ToastProvider,
  useToast,
  ToastViewport,
  ToastMessage,
} from '@simurgh-ui/vue/toast';
import '@simurgh-ui/styles/toast.css';
```

### Angular

```ts
import {
  ToastViewportComponent,
  ToastMessage,
} from '@simurgh-ui/angular/toast';
import '@simurgh-ui/styles/toast.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

ToastProvider owns the queue; the viewport renders polite status announcements. Durations may be disabled for persistent messages, and every toast has a labelled dismiss action.

<ComponentPreview title="Toast">
  <aside class="preview-panel preview-stack" role="status">
    <div class="preview-row">
      <strong>Changes saved</strong>
      <button
        class="preview-button"
        type="button"
        aria-label="Dismiss notification"
      >
        ×
      </button>
    </div>
    <span class="preview-muted">Your profile has been updated.</span>
  </aside>
</ComponentPreview>

<Tabs>
<TabItem label="React">
```tsx
const { toast } = useToast();
toast({ title: 'Saved', description: 'Your changes are live.' });
// Render <ToastViewport /> inside <ToastProvider>.
```
</TabItem>
<TabItem label="Vue">
```ts
const { toast } = useToast();
toast({ title: 'Saved', description: 'Your changes are live.' });
// Render ToastViewport inside ToastProvider.
```
</TabItem>
<TabItem label="Angular">
```ts
@ViewChild(ToastViewportComponent) toasts!: ToastViewportComponent;
this.toasts.toast({ title: 'Saved', description: 'Your changes are live.' });
```
</TabItem>
</Tabs>

Keep messages concise. Use a dialog or inline error when the user must respond before continuing.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Further guidance

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/toast.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/toast.ts).
