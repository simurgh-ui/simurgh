---
title: Button Group
description: Related actions arranged as one named horizontal or vertical control group.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add button-group
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import {
  ButtonGroup,
  ButtonGroupText,
  ButtonGroupSeparator,
} from '@simurgh-ui/react/button-group';
import '@simurgh-ui/styles/button-group.css';
```

### Vue

```ts
import {
  ButtonGroup,
  ButtonGroupText,
  ButtonGroupSeparator,
} from '@simurgh-ui/vue/button-group';
import '@simurgh-ui/styles/button-group.css';
```

### Angular

```ts
import {
  ButtonGroupComponent,
  ButtonGroupTextComponent,
  ButtonGroupSeparatorComponent,
} from '@simurgh-ui/angular/button-group';
import '@simurgh-ui/styles/button-group.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

## Purpose

Button Group visually joins related actions while exposing a single named group to assistive technology. Use the text and separator parts for compact labels or for dividing distinct action sets.

## Anatomy

The public component parts are listed in API surface below. Use only the parts needed by the example;
compound components depend on their documented parent/child nesting.

## Basic usage

<ComponentPreview title="Button Group">
  <div
    role="group"
    aria-label="Text alignment"
    aria-orientation="horizontal"
    data-slot="button-group"
  >
    <button type="button">Left</button>
    <button type="button">Center</button>
    <button type="button">Right</button>
  </div>
</ComponentPreview>

## Examples

<Tabs>
  <TabItem label="React">
    ```tsx
    <ButtonGroup aria-label="Text alignment">
      <Button>Left</Button>
      <Button>Center</Button>
      <Button>Right</Button>
    </ButtonGroup>
    ```
  </TabItem>
  <TabItem label="Vue">
    ```vue
    <ButtonGroup aria-label="Text alignment">
      <Button>Left</Button>
      <Button>Center</Button>
      <Button>Right</Button>
    </ButtonGroup>
    ```
  </TabItem>
  <TabItem label="Angular">
    ```html
    <simurgh-button-group aria-label="Text alignment">
      <simurgh-button>Left</simurgh-button>
      <simurgh-button>Center</simurgh-button>
      <simurgh-button>Right</simurgh-button>
    </simurgh-button-group>
    ```
  </TabItem>
</Tabs>

{/* component-state-model:start */}
## State model

This component exposes no public controlled/uncontrolled state pair. Configure it through the props,
events, native attributes, or parent-owned state documented in the API tables.
{/* component-state-model:end */}

Give every Button Group an accessible name with `aria-label` or `aria-labelledby`. Keep keyboard behavior native to the contained controls; use Toggle Group when the buttons represent persistent selected states.

## API surface

- React exports: `ButtonGroup`, `ButtonGroupText`, `ButtonGroupSeparator`
- Vue exports: `ButtonGroup`, `ButtonGroupText`, `ButtonGroupSeparator`
- Angular exports: `ButtonGroupComponent`, `ButtonGroupTextComponent`, `ButtonGroupSeparatorComponent`

Import these public symbols from the component subpath shown in Installation. Framework-specific
props, events, slots, directives, methods, defaults, and native-attribute behavior belong in the API
tables on this page; use the linked source only to verify the current implementation.

{/* react-api:start */}
### React

An inherited-attributes entry means the component accepts the complete named React native interface,
including its event handlers and ARIA and data attributes. Remaining attributes are forwarded to the
rendered element unless the component behavior described on this page overrides them.

### `ButtonGroup`

Inherited attributes: `HTMLAttributes<HTMLDivElement> & RefAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `orientation` | `Orientation` | `'horizontal'` |
| `ref` | `Ref<HTMLDivElement> \| undefined` | `undefined` |

### `ButtonGroupText`

Inherited attributes: `HTMLAttributes<HTMLSpanElement> & RefAttributes<HTMLSpanElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `ref` | `Ref<HTMLSpanElement> \| undefined` | `undefined` |

### `ButtonGroupSeparator`

Inherited attributes: `HTMLAttributes<HTMLSpanElement> & RefAttributes<HTMLSpanElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `orientation` | `Orientation` | `'vertical'` |
| `ref` | `Ref<HTMLSpanElement> \| undefined` | `undefined` |
{/* react-api:end */}

{/* vue-api:start */}
### Vue

Boolean props without explicit defaults use Vue's `false` default. Undeclared attributes follow
the fallthrough behavior stated for each component.

### `ButtonGroup`

Vue attribute fallthrough is enabled for the rendered root.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `orientation` | `Orientation` | `'horizontal'` |

No emitted events.

Slots: `default`.

Exposed methods: none.

### `ButtonGroupText`

Vue attribute fallthrough is enabled for the rendered root.

No declared props.

No emitted events.

Slots: `default`.

Exposed methods: none.

### `ButtonGroupSeparator`

Vue attribute fallthrough is enabled for the rendered root.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `orientation` | `Orientation` | `'vertical'` |

No emitted events.

Slots: none.

Exposed methods: none.
{/* vue-api:end */}

{/* angular-api:start */}
### Angular

Inputs and outputs use their public template names. Public methods are callable through a template
reference or `ViewChild`. Native attributes apply to the documented template root or directive host;
they are not automatically forwarded through component hosts.

### `ButtonGroupComponent`

Component selector: `simurgh-button-group`. The template's first native element is `ng-content`. Angular host bindings: `{ role: 'group', '[attr.aria-orientation]': 'orientation', '[attr.data-slot]': "'button-group'", }`.

| Input | Type | Default / requirement |
| --- | --- | --- |
| `orientation` | `Orientation` | `'horizontal'` |

No outputs.

Content projection: `default`.

No public methods.

### `ButtonGroupTextComponent`

Component selector: `simurgh-button-group-text`. The template's first native element is `ng-content`. Angular host bindings: `{ '[attr.data-slot]': "'button-group-text'" }`.

No inputs.

No outputs.

Content projection: `default`.

No public methods.

### `ButtonGroupSeparatorComponent`

Component selector: `simurgh-button-group-separator`. The component has no single native template root. Angular host bindings: `{ role: 'separator', '[attr.aria-orientation]': 'orientation', '[attr.data-slot]': "'button-group-separator'", }`.

| Input | Type | Default / requirement |
| --- | --- | --- |
| `orientation` | `Orientation` | `'vertical'` |

No outputs.

Content projection: none.

No public methods.
{/* angular-api:end */}

{/* component-composition:start */}
### Composition contract

Follow the framework example as a structural contract. Root components own shared state; parts that
consume that state must stay under the root (or be attached to projected descendants in Angular).
Parts described as conditional are optional until their corresponding interaction or semantic region
is used. Do not render a state-consuming part by itself.

| Framework | Required parent/child relationship | Conditional parts | Supporting types |
| --- | --- | --- | --- |
| React | Render `ButtonGroup` as the ancestor. Nest the listed parts inside that root so they can read its shared state/context. | `ButtonGroupText`, `ButtonGroupSeparator` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |
| Vue | Render `ButtonGroup` as the ancestor. Nest the listed parts inside that root so they can read its shared state/context. | `ButtonGroupText`, `ButtonGroupSeparator` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |
| Angular | Create `ButtonGroupComponent` first. Attach listed directives to elements inside its projected content; nested component parts must remain inside it. | `ButtonGroupTextComponent`, `ButtonGroupSeparatorComponent` are conditional parts: include only those needed by the documented anatomy. A trigger/control and its matching content/item are required when that interaction is used. | None. |

Accessible names, descriptions, and form labels remain required whenever the component's purpose
cannot otherwise be determined, even when the corresponding visual part is optional.
{/* component-composition:end */}

{/* framework-parity:start */}
### Framework parity and differences

The adapters target the same user-visible behavior and accessibility contract. Their public shapes
follow each framework's conventions and are not expected to be symbol-for-symbol identical.

| Concern | Contract |
| --- | --- |
| Public composition | All adapters export 3 public symbols, with framework-idiomatic names. |
| State and change events | No controlled state contract; use native attributes, inputs, or events documented above. |
| Children and content | React uses `children`; Vue uses the slots listed above; Angular uses the documented content projection selectors. |
| Native attributes | React forwards the named native interface; Vue follows the stated fallthrough rule; Angular attributes apply to the component host unless an input, directive, or documented native root consumes them. |
| Imperative access | React forwards native refs; Vue exposes no methods; Angular exposes no public methods. |

These differences are intentional adapter design. Behavioral or accessibility differences not stated
on this page are parity defects rather than supported variations.
{/* framework-parity:end */}

{/* component-form:start */}
### Form integration

This component is not a form control and does not contribute a named value to `FormData`. Use it
to structure or describe a form only when its purpose and accessibility guidance apply.
{/* component-form:end */}

{/* component-supported-states:start */}
### Supported states

The table distinguishes component behavior from application-owned presentation. “Not supported”
means there is no public state contract for that adapter; do not invent one with an undocumented
attribute. Keep status and error messages accessible when they are rendered outside the component.

| State | React | Vue | Angular |
| --- | --- | --- | --- |
| Loading | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
| Empty | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
| Invalid | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
| Read-only | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
| Disabled | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
| Error | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. | Not supported by this component API; handle this state in surrounding application UI. |
{/* component-supported-states:end */}

## Customization

Load the optional component stylesheet for the default recipe, override semantic tokens for broad
theme changes, or omit the recipe CSS for headless styling. See [theming and styling
hooks](/guides/theming/) for import order, selectors, dark mode, RTL, and reduced-motion guidance.

## Accessibility

Preserve the documented composition and accessible names when wrapping or restyling this component.
See [accessibility and RTL guidance](/guides/accessibility-rtl/) for keyboard, focus, labeling, and
directionality requirements.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Related components

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/button-group.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/button-group.ts).
