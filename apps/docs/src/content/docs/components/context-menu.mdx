---
title: Context Menu
description: Pointer-positioned contextual actions with complete keyboard access.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ComponentPreview from '../../../components/ComponentPreview.astro';

{/* component-installation:start */}
## Installation

Copy editable source into an initialized application:

```sh
pnpm dlx @simurgh-ui/cli add context-menu
```

For package consumption, import from the component subpath and load its optional recipe CSS:

### React

```tsx
import {
  ContextMenu,
  ContextMenuTrigger,
  ContextMenuContent,
  ContextMenuItem,
} from '@simurgh-ui/react/context-menu';
import '@simurgh-ui/styles/context-menu.css';
```

### Vue

```ts
import {
  ContextMenu,
  ContextMenuTrigger,
  ContextMenuContent,
  ContextMenuItem,
} from '@simurgh-ui/vue/context-menu';
import '@simurgh-ui/styles/context-menu.css';
```

### Angular

```ts
import {
  ContextMenuComponent,
  ContextMenuItemDirective,
} from '@simurgh-ui/angular/context-menu';
import '@simurgh-ui/styles/context-menu.css';
```

Omit the component stylesheet for fully headless styling. CLI-copied components use the local path
written to `simurgh.json` and the copied application styles instead of these package imports.
{/* component-installation:end */}

## Purpose

Context Menu opens at the pointer on right-click and beside its trigger from Shift+F10 or the Menu key. Focus enters the first enabled item, arrow keys navigate, and Escape dismisses the menu.

## Anatomy

The public component parts are listed in API surface below. Use only the parts needed by the example;
compound components depend on their documented parent/child nesting.

## Basic usage

<ComponentPreview title="Context Menu">
  <div>
    <div
      tabindex="0"
      aria-haspopup="menu"
      aria-expanded="true"
      data-slot="context-menu-trigger"
    >
      Right-click this canvas
    </div>
    <div
      role="menu"
      aria-label="Canvas actions"
      class="simurgh-content"
      data-slot="context-menu-content"
    >
      <div role="menuitem" tabindex="0" class="simurgh-item">
        Copy
      </div>
      <div role="menuitem" tabindex="-1" class="simurgh-item">
        Duplicate
      </div>
    </div>
  </div>
</ComponentPreview>

## Examples

<Tabs>
<TabItem label="React">
```tsx
<ContextMenu>
  <ContextMenuTrigger>Canvas</ContextMenuTrigger>
  <ContextMenuContent aria-label="Canvas actions">
    <ContextMenuItem onSelect={copy}>Copy</ContextMenuItem>
    <ContextMenuItem onSelect={duplicate}>Duplicate</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```
</TabItem>
<TabItem label="Vue">
```vue
<ContextMenu>
  <ContextMenuTrigger>Canvas</ContextMenuTrigger>
  <ContextMenuContent aria-label="Canvas actions">
    <ContextMenuItem @select="copy">Copy</ContextMenuItem>
    <ContextMenuItem @select="duplicate">Duplicate</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```
</TabItem>
<TabItem label="Angular">
```html
<simurgh-context-menu>
  <span trigger>Canvas</span>
  <button simurghContextMenuItem (select)="copy()">Copy</button>
  <button simurghContextMenuItem (select)="duplicate()">Duplicate</button>
</simurgh-context-menu>
```
</TabItem>
</Tabs>

## State model

Keep the same actions available through visible controls when they are essential to completing a task; context menus should be a shortcut, not the only path.

## API surface

- React exports: `ContextMenu`, `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`
- Vue exports: `ContextMenu`, `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`
- Angular exports: `ContextMenuComponent`, `ContextMenuItemDirective`

Import these public symbols from the component subpath shown in Installation. Framework-specific
props, events, slots, directives, methods, defaults, and native-attribute behavior belong in the API
tables on this page; use the linked source only to verify the current implementation.

{/* react-api:start */}
### React

An inherited-attributes entry means the component accepts the complete named React native interface,
including its event handlers and ARIA and data attributes. Remaining attributes are forwarded to the
rendered element unless the component behavior described on this page overrides them.

### `ContextMenu`

Complete props type: `PropsWithChildren<OpenProps>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `defaultOpen` | `boolean` | `undefined` |
| `onOpenChange` | `(open: boolean) => void` | `undefined` |
| `open` | `boolean` | `undefined` |

### `ContextMenuTrigger`

Inherited attributes: `HTMLAttributes<HTMLDivElement> & RefAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `ref` | `Ref<HTMLDivElement> \| undefined` | `undefined` |

### `ContextMenuContent`

Inherited attributes: `HTMLAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |

### `ContextMenuItem`

Inherited attributes: `HTMLAttributes<HTMLDivElement> & RefAttributes<HTMLDivElement>`.

| Prop | Type | Default / requirement |
| --- | --- | --- |
| `children` | `ReactNode` | `undefined` |
| `disabled` | `boolean` | `undefined` |
| `onSelect` | `ReactEventHandler<HTMLDivElement> & (() => void)` | `undefined` |
| `ref` | `Ref<HTMLDivElement> \| undefined` | `undefined` |
{/* react-api:end */}

## Customization

Load the optional component stylesheet for the default recipe, override semantic tokens for broad
theme changes, or omit the recipe CSS for headless styling. See [theming and styling
hooks](/guides/theming/) for import order, selectors, dark mode, RTL, and reduced-motion guidance.

## Accessibility

Preserve the documented composition and accessible names when wrapping or restyling this component.
See [accessibility and RTL guidance](/guides/accessibility-rtl/) for keyboard, focus, labeling, and
directionality requirements.

{/* doc-verification:start */}
_Last verified on 2026-08-12 against [Simurgh registry 0.1.0](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json)._
{/* doc-verification:end */}

## Related components

- Review the shared [accessibility and RTL guidance](/guides/accessibility-rtl/) and
  [theming and styling hooks](/guides/theming/).
- Use the [component chooser](/guides/component-chooser/) and
  [component overview](/components/overview/) to compare related primitives.
- Inspect the registry [manifest](https://github.com/simurgh-ui/simurgh/blob/main/packages/registry/registry.json) and framework
  source for [React](https://github.com/simurgh-ui/simurgh/blob/main/packages/react/src/index.tsx),
  [Vue](https://github.com/simurgh-ui/simurgh/blob/main/packages/vue/src/components/context-menu.ts), or
  [Angular](https://github.com/simurgh-ui/simurgh/blob/main/packages/angular/src/components/context-menu.ts).
