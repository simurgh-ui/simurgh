---
'@simurgh-ui/cli': minor
---

Copy a separate CSS recipe for each CLI-installed component and maintain a stable `recipes.css`
index containing only the installed component imports. Preserve customized recipe files unless
`--overwrite` is used.
